# Phase 15 exposure band (cold). Applied and removed by modules/exposure;
# the module refreshes it every tick (a short-lived, non-permanent buff, so
# the engine's permabuff reconciliation never strips it). No vitality
# penalty: that would shrink max HP and turn a penalty band lethal.
buffid: 1010
name: Chilled
description: You are chilled to the bone.
secret: false
triggerrate: 1 round
triggercount: 11
statmods:
  speed: -5
  perception: -5
