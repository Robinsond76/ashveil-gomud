# Phase 15 exposure band (heat). Applied and removed by modules/exposure;
# the module refreshes it every tick (a short-lived, non-permanent buff, so
# the engine's permabuff reconciliation never strips it). No vitality
# penalty: that would shrink max HP and turn a penalty band lethal.
buffid: 1021
name: Heatstricken
description: The heat makes your head swim.
secret: false
triggerrate: 1 round
triggercount: 11
statmods:
  speed: -15
  strength: -15
  perception: -15
